# P01–P02 与 mock 登录

用户授权在 feature/base 实现登录与手机号验证，AppID 和短信未就绪，使用 mock。

- 按已读取的 Figma 27:210、27:221 还原 uni-app 页面；P02 未登录顶部改为返回栏，补验证码发送按钮。
- 开发默认启用 mock，生产默认关闭，可显式配置用于演示；不调用微信或短信，不伪装真实认证。
- 独立 mock 服务覆盖手机号校验、发送限频、验证码错误/过期/单次消费、微信需绑定、未准入与停用；测试先行。
- 登录会话仅内存保留，刷新后回到登录；登录成功进入现有首页并展示身份，支持退出。真实业务页面仍待实现。
- 校验类型、行为测试、微信/H5 构建与浏览器交互。保留本地修改，不自动提交推送。

## 验证结果

已完成上述范围。`npm test` 3 组测试通过；`npm run typecheck`、`npm run build:mp-weixin`、`npm run build:h5` 均通过。

使用 Chrome/Playwright 在 H5 验证手机号登录、错误验证码重试、退出、微信模拟绑定、停用账号提示、刷新清理会话及 320px 宽度无横向溢出；无页面运行时错误。390×844 截图对照了 P01/P02，并修正 P02 标签盒模型使按钮回到设计位置。生产产物检查确认 mock 默认关闭。

微信编译通过但未进行开发者工具/真机验证；真实微信与短信服务尚未接入。P02 顶部改为未登录返回栏，添加获取验证码按钮；其余业务页保持占位。
