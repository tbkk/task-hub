# 标准版开发执行记录

2026-09-20 用户授权在当前 `feature/base` 完成全部计划；九识暂未具备测试资源，先完成模拟闭环。规则与范围见 `../specs/2026-09-20-delivery-scope.md`。

| 工作流 | 状态 | 证据 |
| --- | --- | --- |
| 阶段 1 基础交互 | 已完成本地验证 | 整体路线图阶段 1；9 项测试、三项类型/构建检查、浏览器回归 |
| 后端接口与任务索引 | 已编写，逐切片补充代码式计划 | `docs/api/delivery-contract.md`、`2026-09-20-server-delivery.md` |
| 管理平台任务索引 | 已编写，逐切片补充代码式计划 | `2026-09-20-admin-delivery.md` |
| 小程序业务任务索引 | 已编写，逐切片补充代码式计划 | `2026-09-20-mini-delivery.md` |
| 迁移/API基础 S01 | 实现及本地验证通过，待 review | 8 项 Maven 测试、MigrationIT 真实 MySQL V1/重复执行/sentinel 保留 |
| 身份后端 S02–S04 | 实现、联调与复审通过 | `2026-09-20-server-identity-implementation.md` |
| 主数据后端 S05 | 实现与真实库验证通过，待整体审查 | MasterdataIT 10项；管理端真实接口联调 |
| 订单后端 S06–S08 | 首版实现并验证，待审查 | V4/V5、幂等/事件/预约/收藏/订单/取消；全套64项 |
| 仓库受理 S09 | 首版实现，未组批流程验证通过 | 受理/驳回/独立取消处理/容量一次释放；待审查 |
| 组批装载 S10/S11 | 实现、回归及审查修复通过 | BatchIT8项；LOCKED和换车失效覆盖 |
| 模拟提供者/派发/遥测 S12–S14 | 首版实现及真实库验证通过，待独立审查 | SimulatorIT4、DispatchIT8、VehicleEventIT4 |
| 取货与车辆控制 S15/S16 | 未开始 | 后续模拟闭环关键路径 |
| 工单/消息 S17/S18 | 基础实现及定向验证通过，待审查/控制接线 | TicketIT2、NotificationIT2 |
| 小程序工人页面 | P03–P09/P19实现并通过接口测试 | 29项测试、类型检查、双构建、320/390px浏览器 |
| 小程序仓库审批/批次页面 | 实现、修复与真实浏览器验证通过 | 35项mini测试、类型/双构建；320/390px |
| 小程序装货/调度/取货/工单/消息/概览 | 待完成 | 后续前端切片 |
| 管理平台人员/主数据/规则页面 | 实现与联调通过，待整体审查 | 15项测试、构建、真实MySQL浏览器 |
| 管理报表/审计/接入页面 | 未开始 | 依赖后续接口 |
| 跨端模拟闭环、导出与恢复 | 未开始 | 依赖业务实现 |
| 微信/短信/实车验收 | 外部依赖阻塞 | 缺少凭证、测试资源；不计作模拟交付完成 |

每个业务任务追加测试与 review 结果；已有阶段不重复执行。当前尚未提交、推送或合并。

## 2026-09-20 验证入口与迁移基础

- 新增 `harness/scripts/verify.sh`，Bash 语法检查通过，非法模式退出 2；从 `/tmp` 调用管理端验证成功，3 测试及构建通过。
- S01 RED：`mvn -Dtest=ApiErrorTest test -q` 因缺少 ApiException、PageResponse、Flyway 编译失败；实现后 `mvn test -q` 8 测试通过。
- 显式专用库 `task_hub_delivery_test` 在本地 MySQL 9.6.0 创建，MigrationIT 应用 V1 并成功重复执行（0 新迁移），保留 sentinel 元数据。未修改其他数据库。
- Flyway 对本机 MySQL 9.6 提示版本未验证；目标 MySQL 8.4 兼容性仍需独立验证，不将本机成功作为目标版本验收。
- 新增 A01–A15 矩阵，修正总体路线图遗漏 A14 消息报表、A15 首屏分页的编号范围。

## 2026-09-20 三端身份联调

- 服务端身份切片测试 36 项、打包通过；独立审查发现相同密码绕过首次改密及短信验证并发读取旧员工信息，待修复与回归后再标记完成。
- 管理端 9 项测试和构建通过；真实浏览器验证内部账号登录 → 强制改密 → 重新登录 → 身份查询 → 服务端退出。
- 小程序修复退出后迟到响应恢复身份、旧会话 401 清除新会话及角色撤权误注销全部会话。22 项测试、类型检查、H5 和微信构建通过。
- 使用管理 API 创建隔离演示员工；H5 浏览器经服务端模拟短信挑战完成登录，进入真实 MySQL 身份对应的工人工作区。未将纯前端 fixture 作为该验证的数据源。
- 实测数据库为 MySQL 9.6.0，目标 8.4 未执行。微信提供者、短信发送服务及车辆 API/MQTT 仍为模拟/外部依赖阻塞。
- 工人归属仓库补充决策：employee 增加可空 home_warehouse_id，worker SELF 仍仅表示本人订单范围；未配置时不开放申请仓库。后续主数据迁移与员工维护将同步实现。

## 2026-09-21 继续执行

- 身份审查三项问题已修复并独立复审通过。真实 MySQL 锁交错先复现旧号码/停用/微信绑定并发漏洞，再验证修复；同密码绕过及错误信封也覆盖回归。最终 43 项测试全部通过，打包通过。
- 人员页面首版和 API 适配已实现，管理端 12 项测试及构建通过。浏览器发现抽屉 size CSS 函数被组件解析为 0 宽，改为固定宽度配合最大视口宽度；继续交互验证。
- 主数据 S05 开始实施，使用 V3 新增表与 employee.home_warehouse_id；不改已有 V2。

- 员工目录新增归属仓库持久化、只读微信绑定状态、角色/仓库/验证/绑定筛选，以及人员管理权限专用仓库选项。EmployeeDirectoryIT、IdentityIT、AuthorizationIT 共 16 项通过；RED 曾复现未知仓库被忽略、筛选未生效。最新界面仍需与更新后服务联调。
- S05 实现与独立验证：MasterdataIT 9 项通过；与 EmployeeDirectoryIT 合跑共 12 项通过。覆盖版本冲突、范围隔离、可达目录、规则格式、硬件同步与生产模拟保护。活动业务占用阻断、slots 容量和收藏仍待订单切片，不计为已完成。

## 2026-09-21 订单核心进展

- V4__business_foundation.sql：持久化幂等、审计和业务事件；V5__orders.sql：预约容量、订单、取消申请、收藏。迁移在专用MySQL库实测，保留既有数据。
- IdempotencyIT 5项及MigrationIT通过，覆盖并发只执行一次、正文冲突、授权变化不重放、失败回滚及审计脱敏/事件同事务。
- OrderIT首轮404 RED后实现目录时段/创建/详情/履历/取消/收藏，后续3项通过，包含容量1两个并发请求仅一次成功、第三人404、重复提交一单、取消不改变主状态。已有取消再次申请预期200实际409已补RED→GREEN回归。
- 全套 Maven64项通过（包含真实MySQL IT），管理端15项测试及构建通过。日期JDBC LocalDateTime/Timestamp差异已修复为UTC读取。
- 浏览器真实管理流程验证车辆编辑、硬件同步、标签维护和规则保存。模拟目录仅填充专用演示库，业务记录不使用内存mock。
- 以上代码待整体审查；仓库受理、组批装货、车辆模拟任务、取货、工单、消息报表与完整闭环尚未完成。
- 真实小程序浏览器联调通过：服务端短信模拟认证→账号归属仓库→可达点位→未来时段→订单落库→详情履历→独立取消申请；脚本 worker-real-orders.cjs，无接口拦截。
- UTC回归先复现MySQL默认时间比UTC偏移28800秒，再将连接设为 connectionTimeZone=UTC&forceConnectionTimeZoneToSession=true；完整65项测试通过。早期本地演示/测试记录的默认时间可能保留旧偏移，未盲目批量修历史字段；新业务连接统一UTC。
- S06当前为幂等/审计/事件基础及订单接入；员工/主数据/登录等审计接入、事件投递消费尚未完成，不能视作全局审计闭环。

- S09 未组批订单受理/驳回/取消处理已实现，OrderIT扩展至5项通过，包含驳回同key重放不重复释放、受理后取消批准仍按独立状态处理。已进入装载或运输的取消当前保守拒绝，待批次/车辆模块实现安全处理。
- 最新UTC连接已重新打包并重启本地服务，真实小程序申请→履历→取消流程再次通过。
- 仍在 feature/base，未提交、推送、合并；主数据/管理页/订单新增代码需后续独立审查，不能将未审查切片标为整体完成。

## 2026-09-21 仓库批次与模拟车辆基础

- S10/S11新增V6：同仓同点同时段组批、成员历史、独立格口占用、人工装货确认；取消已组批订单安全移出并使装货失效，最后成员取消显式关闭空批。BatchIT首轮6项、OrderIT5项、MasterdataIT10项共21项通过；独立审查要求补LOCKED直接编辑与换车失效回归，补齐后BatchIT8项通过，待复审。
- 仓库小程序Task5新增审批/取消处理、批次分页/编辑、并发冲突刷新、未知结果同key重试；34项测试、类型检查及H5/微信双构建通过，真实浏览器联调尚待执行。
- S06–S09独立审查发现履历日期筛选和验证遗漏；已补上海业务日from/to筛选、跨仓/空原因/取消拒绝、两人审批一次成功、动作审计事件不重复与事务故障回滚。OrderIT9+IdempotencyIT6共15项通过，待独立复审。
- S12提供者基础新增V7：持久化模拟场景/命令、同requestId重放/正文冲突、TIMEOUT保持UNKNOWN，生产禁止模拟。SimulatorIT4项通过；事件融合/车辆快照查询仍待S14，不能将S12标为全部完成。
- 补齐S13明确装货点前置：V8及管理GET/PUT /admin/warehouses/{id}/loading-stop，版本/本仓启用点位/范围校验、绑定使用保护、事务审计。LoadingStopIT通过；后台配置入口构建通过，真实API保存/重读与390px对话框检查通过。独立review尚待进行。
- 当前本地后端已打包重启并对演示库原位升级至V8；MySQL9.6的Flyway兼容提示及后台chunk体积提示仍存在，目标MySQL8.4和真实微信/短信/实车未验。仍未提交、推送、合并。

## 2026-09-21 本轮综合验证与剩余范围

- 服务端 `mvn -Dtest=*Test,*IT test`：100项通过，0失败/错误/跳过；包含真实MySQL数据库测试。日志 `/tmp/task-hub-latest-full-tests.log`。
- 小程序35项测试、类型检查、H5及微信构建通过。仓库真实浏览器验证登录→有取消申请时仍正确受理原订单→拒绝取消→组批落库；390/320px通过。修复操作路由、未知请求重试正文与busy响应式，候选订单有后续页入口，长订单号独立行、状态中文。
- 管理端15项测试、构建通过；新增装货点配置真实API保存/重读及390px验证通过。
- S13/S14首版完成派发请求/任务、UNKNOWN保留锁、不自动重发、遥测独立判序/去重/旧任务隔离、正确到站推进单据；仍待独立review。任务详情的到离站时刻及控制履历聚合、主动外部快照查询尚待后续接线。
- S17基础工单独立生命周期与账号/仓库范围、并发关闭、重复提交已验证；处理原因限制1000字与审计字段对齐。尚待控制共锁竞态及前端。
- S18消息持久化投递、账号去重、失败回滚重试、晚验证收货人、失权目标裁剪、幂等已读已验证；NOTIFICATIONS_ENABLED显式调度，尚待独立review、进程重启与小程序联调。
- 订单/批次复核见 `docs/reviews/2026-09-21-orders-batches-review.md`；本轮不宣称全部开发计划完成。S15/S16、S19/S20、其余前端页面及完整模拟验收/恢复仍待开发，真实外部验收继续阻塞。
- 本地最新服务已启动（PID32105，8080），演示库原位升级V11，ready返回READY；local启用车辆模拟和站内消息调度。真实账号请求消息接口读取16条持久化通知，调度投递可用；这不是完整进程重启恢复验收。
