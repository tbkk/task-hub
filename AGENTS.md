# Task Hub 开发规则

本文件供开发者和编码 agent 使用。2026-09-20 按用户要求，参考 `/Users/qin/Projects/puck/AGENTS.md` 及其 `.agent/workflows/` 中的开发规范制定；仅借鉴需求与工程流程，不引入 Puck 的业务规则、技术栈或运行配置。

## 开始工作

- 项目文档统一使用中文，包括需求、设计、开发计划、验证记录、review 和变更说明。
- 开始任务前阅读本文件、`docs/README.md`、整体开发计划及相关模块文档，再查看现有实现。
- 当前总体进度与阶段验收记录维护在 `docs/superpowers/plans/2026-09-20-development-roadmap.md`。
- 详细执行流程见 `docs/development/workflow.md`。

## 新功能流程

- 按“PRD → 设计文档 → 开发计划 → 编码 → 验证 → review → 提交合并”推进。
- 已有明确需求、设计或计划可以引用并补充，不重复编写同一份材料；需求变更时先同步对应文档。
- PRD 明确目标、范围、非目标、验收标准与异常场景；设计说明接口、数据、权限、错误处理与测试策略；计划拆分为可独立验证的任务。
- 新功能从 `dev1.0` 创建 `feature/` 分支或隔离 worktree；不直接在 `dev1.0` 或主分支开发。已在对应 feature 分支的任务继续使用该分支。
- 实现前查找相近模块并复用现有模式，保持修改范围与需求一致。

## Bug 修复

- 可直接在当前分支修复，无需另建 feature 分支或补齐 PRD、设计、计划三件套。
- 先复现或定位根因，再做最小修复，完成必要的回归验证、受影响模块验证和 review。
- 数据库、权限、并发或安全问题应补充专项验证；涉及结构变化时新增迁移。

## 实现与验证

- 控制器保持薄层，业务逻辑放服务层；前端请求集中在 API/服务适配层，页面负责展示与交互编排。
- 复用现有组件和样式；可点击操作提供即时反馈，覆盖加载、空状态、失败和重复操作。
- 数据库变更新增有序 migration，兼容已有数据；记录实际执行环境、结果与恢复方案，不通过重建数据库替代升级。
- 按修改范围执行测试、类型检查和构建；具体命令见开发流程。界面改动补充交互和布局检查。
- 验证结果区分“通过”“未执行”“依赖阻塞”；Mock、编译或 H5 通过不等于真实接口、微信真机或实车验收通过。
- 完成 review，修复确认的问题并复验受影响范围，才能宣称该任务完成。

## 版本与协作

- 提交、推送、合并按用户授权执行；流程要求本身不视为发布或合并授权。
- feature 完成后先合并到 `dev1.0`，在合并后的 `dev1.0` 运行集成验证；发布合并到主分支后，在主分支再次验证。
- 不覆盖、回退或清理用户及其他协作者的未提交修改。
- 文件搜索优先使用 `rg` / `rg --files`，手动编辑优先 patch。
- 不将密钥、token、真实密码写入源码、文档或普通日志。
- 交付说明写清改动、验证证据、未完成项与外部依赖，并同步相关文档。
