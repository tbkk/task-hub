# Copy to deploy/.env and replace both passwords before starting.
MYSQL_DATABASE=task_hub
MYSQL_USER=task_hub
MYSQL_PASSWORD=replace-with-a-local-app-password
MYSQL_ROOT_PASSWORD=replace-with-a-different-root-password
WEB_PORT=8088
